<!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top">
        <div class="container">
        <%= link_to root_path, class: "navbar-brand text-decoration-none" do %>
            <i class="fas fa-drum me-2"></i>Percusion Ecuador
        <% end %>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <%= link_to "Home", root_path, class: "nav-link" %>
                    </li>

                    <li class="nav-item dropdown">
                        <button class="nav-link dropdown-toggle" id="categoriasDropdown" data-bs-toggle="dropdown">
                            Categories
                        </button>
                        <ul class="dropdown-menu">
                        <% @categories.each do |category| %>
                            <li>
                                <%= link_to category_path(category), class: "dropdown-item" do %>
                                    <i class="fas fa-drum me-2"></i><%= category.name %>
                                <% end %>
                            </li>
                        <% end %>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Products", products_path, class: "nav-link" %>

                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#nosotros">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contacto">Contact</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center">
                    <a href="#" class="btn btn-outline-primary me-3">
                        <i class="fas fa-user me-1"></i>Sign In
                    </a>


                    <%= link_to cart_index_path, class: "position-relative me-3" do %>
                        <i class="fas fa-shopping-cart fa-lg text-primary"></i>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-info">3</span>
                    <% end %>



                    <a href="https://wa.me/593996888655" class="social-link whatsapp d-flex align-items-center justify-content-center" target="_blank">
                        <i class="fab fa-whatsapp"></i>
                    </a>
                </div>
            </div>
        </div>
    </nav>